stages:
  - build

build_project:
  stage: build
  script:
    - xcodebuild clean -project QuarantineApp.xcworkspace -scheme QuarantineApp | xcpretty
    - xcodebuild test -project QuarantineApp.xcworkspace -scheme QuarantineApp -destination 'platform=iOS Simulator,name=iPhone 11 Pro Max,OS=13.2.2' | xcpretty -s
  tags:
    - ci

